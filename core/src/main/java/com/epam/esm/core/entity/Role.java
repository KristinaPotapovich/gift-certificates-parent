package com.epam.esm.core.entity;

/**
 * Enum User role.
 */
public enum Role {
    /**
     * Admin role.
     */
    ADMIN,
    /**
     * User role.
     */
    USER,
    /**
     * Guest role.
     */
    GUEST
}
